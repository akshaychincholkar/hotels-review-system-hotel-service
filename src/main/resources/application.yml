spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/hotels_review_system
    username: postgres
    password: root
#    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
  show-sql: true

server:
  port: 8082